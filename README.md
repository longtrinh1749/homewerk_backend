# homewerk_backend

## Prerequisites 
* Python 3.8
* MySQL 8.0
* Docker (optional)
## Configuration
Install libraries  
* python -m venv venv
* source venv/bin/activate
* pip install --upgrade pip
* pip install -r requirements.txt
